/**
 * App-level toggles.
 */

/**
 * Login wall.
 *
 * false = anyone can browse every page without signing in (demo mode).
 * true  = MENTOR·OS is login-walled; /login is the only way in.
 *
 * ⚠️ TEMPORARILY false for a team demo — flip back to `true` to
 * re-enable the auth gate. Nothing else needs changing.
 */
export const AUTH_REQUIRED = false;
