import { LandingDesktop } from "@/components/landing/landing-desktop";

/**
 * Marketing landing = the OS itself. BIOS boot sequence, then a
 * desktop of draggable windows: WELCOME.TXT hero, FEATURES.DLL,
 * DISPLAY PROPERTIES (live theme switcher) and friends.
 */
export default function LandingPage() {
  return <LandingDesktop />;
}
